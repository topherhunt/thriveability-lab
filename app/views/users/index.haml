.row
  .col-md-4
    %h1 People
    %p All people are part of our growing community of creative change-makers. Use this page to get to know others, and introduce yourself to them.
    %p
      - unless user_signed_in?
        = link_to "Join the community", new_user_registration_path, class: "test-signup-button btn btn-success"
      = link_to "All people", search_users_path, class: "test-all-users-button btn btn-primary"

    .hidden-sm.hidden-xs{style: "padding-top: 30px;"}
      = image_tag "list_images/people#{[1,2].shuffle.first}.jpg", style: "width: 100%;"

  .col-md-8
    .row
      .col-sm-7
        %h3 Recently active
        - @most_active_users.each do |user|
          = render partial: "list_row", locals: {user: user, opts: {}}
        %hr
        %p= link_to "See all", search_users_path

      .col-sm-5
        %h3 Recent activity
        = render partial: "shared/recent_events", locals: { events: @recent_events}
