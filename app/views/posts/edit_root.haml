.centered-800
  %h1
    - if @post.published?
      Edit post: "#{link_to @post.title, post_path(@post)}"
    - else
      Start a conversation!

  = render partial: "form_root", locals: { post: @post }
