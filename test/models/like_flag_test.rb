require "test_helper"

class LikeFlagTest < ActiveSupport::TestCase
  context "counter caches" do
    test "project counter cache stays up-to-date" do
      # PENDING
    end

    test "resource counter cache stays up-to-date" do
      # PENDING
    end

    test "user counter cache stays up-to-date" do
      # PENDING
    end
  end
end
